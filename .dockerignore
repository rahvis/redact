# Keep the build context small. NOTE: README.md must stay — pyproject.toml
# references it as the project readme, so pip needs it during install.
.git
.gitignore
Screenshots/
appimage/
flatpak/
snapcraft.yaml
docs/
# Local output folders created by desktop app runs (current or legacy name)
*-files/
*.ico
*.svg
